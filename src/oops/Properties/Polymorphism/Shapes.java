package oops.Properties.Polymorphism;

public class Shapes {
    void area(){
        System.out.println("I am in shapes");
    }

    // if a method is final then it can't be overridden
    //Early bindings: check notes

    // if a class is final, it can't be inherited
    //whenever a class is declared final, implicitly its methods are also declared final
}
